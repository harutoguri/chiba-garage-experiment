# v8 実機テスト問題分析

## 動画1: 車両間スクロール
- 黒画面が長時間表示される（特に連続スクロール時）
- 原因: 現在の実装はscroll-snap containerで縦スクロールしているが、各スライドのvideo要素はIntersectionObserverで表示時にHLS再生を開始する。HLSの初期化に時間がかかるため黒画面になる。
- 解決策: 
  1. 前後1スライドのvideo要素をプリロード（表示前にHLS初期化だけしておく）
  2. 画像の1枚目をvideo背景として表示し、HLS再生開始まで画像でカバー
  3. 動画の最初のフレームをサムネイルとして使用

## 動画2: 横スクロール
- 横スクロールがscroll-snapではなくJavaScriptのtouchイベントで実装されている
- 指の動きに追従しない（即差し替えのため）
- 横スワイプが縦スワイプと誤判定される
- 解決策:
  1. 横スクロールもscroll-snap container（x mandatory）で実装
  2. 各スライド内にネストされたscroll-snap x containerを配置
  3. これにより指の動きに追従し、縦/横の判定もブラウザに任せられる

## 追加要件
- 詳細文章を「車両詳細」ボタンに畳む
- タップで詳細を拡大表示できるようにする
